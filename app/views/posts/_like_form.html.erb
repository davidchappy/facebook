<%= form_for @post.likes.build, html: { class: 'new_like text-only'} do |f| %>
  <%= f.hidden_field :post_id, value: @post.id %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  
  <%= f.submit 'LIKE', class: 'text-only' %>
<% end %>