<%= friendship.user.name %>
<% if friendship.status == "pending" %>
  (<%= link_to "accept", friendship_path(friendship, @user), method: :patch, action: :update %>)
<% else %>  
  (<%= link_to "remove", friendship, :method => :delete %>)
<% end %>   